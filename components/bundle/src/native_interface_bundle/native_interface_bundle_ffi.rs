// automatically generated by rust-bindgen 0.71.1

#![allow(non_upper_case_globals)]
#![allow(non_camel_case_types)]
#![allow(non_snake_case)]
#[cfg(feature = "api-21")]
use crate::ability_resource_info::OH_NativeBundle_AbilityResourceInfo;
#[cfg(feature = "api-21")]
use crate::bundle_manager_common::BundleManager_ErrorCode;

/// Indicates information of application
///
///
/// Required System Capabilities: SystemCapability.BundleManager.BundleFramework.Core
///
/// Available since API-level: 9
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct OH_NativeBundle_ApplicationInfo {
    /// Indicates the name of application
    ///
    /// Required System Capabilities: SystemCapability.BundleManager.BundleFramework.Core
    ///
    /// Available since API-level: 9
    pub bundleName: *mut ::core::ffi::c_char,
    /// Indicates the fingerprint of application
    ///
    /// Required System Capabilities: SystemCapability.BundleManager.BundleFramework.Core
    ///
    /// Available since API-level: 9
    pub fingerprint: *mut ::core::ffi::c_char,
}
/// Indicates information of elementName.
///
///
/// Available since API-level: 13
#[cfg(feature = "api-13")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-13")))]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct OH_NativeBundle_ElementName {
    /// Indicates the name of application.
    pub bundleName: *mut ::core::ffi::c_char,
    /// Indicates the name of module.
    pub moduleName: *mut ::core::ffi::c_char,
    /// Indicates the name of ability.
    pub abilityName: *mut ::core::ffi::c_char,
}
/// Indicates information of metadata.
///
///
/// Available since API-level: 20
#[cfg(feature = "api-20")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
#[repr(C)]
#[derive(Debug, Copy, Clone)]
pub struct OH_NativeBundle_Metadata {
    /// Indicates the metadata name.
    pub name: *mut ::core::ffi::c_char,
    /// Indicates the metadata value.
    pub value: *mut ::core::ffi::c_char,
    /// Indicates the metadata resource.
    pub resource: *mut ::core::ffi::c_char,
}
/// Indicates information of module metadata.
///
///
/// Available since API-level: 20
#[cfg(feature = "api-20")]
#[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
#[repr(C)]
pub struct OH_NativeBundle_ModuleMetadata {
    /// Indicates the moduleName of module.
    pub moduleName: *mut ::core::ffi::c_char,
    /// Indicates the metadata array of module.
    pub metadataArray: *mut OH_NativeBundle_Metadata,
    /// Indicates the metadata array size of module.
    pub metadataArraySize: usize,
}
extern "C" {
    /// Obtains the application info based on the The current bundle.
    ///
    ///
    /// # Returns
    ///
    /// * Returns the newly created OH_NativeBundle_ApplicationInfo object, if the returned object is NULL,
    /// it indicates creation failure. The possible cause of failure could be that the application address space is full,
    /// leading to space allocation failure.
    ///
    /// Available since API-level: 9
    ///
    /// Version: 1.0
    pub fn OH_NativeBundle_GetCurrentApplicationInfo() -> OH_NativeBundle_ApplicationInfo;
    /// Obtains the appId of application. AppId indicates the ID of the application to which this bundle belongs
    /// The application ID uniquely identifies an application. It is determined by the bundle name and signature.
    /// After utilizing this interface, to prevent memory leaks,
    /// it is necessary to manually release the pointer returned by the interface.
    ///
    ///
    /// # Returns
    ///
    /// * Returns the newly created string that indicates appId information,
    /// if the returned object is NULL, it indicates creation failure.
    /// The possible cause of failure could be that the application address space is full,
    /// leading to space allocation failure.
    ///
    /// Available since API-level: 11
    ///
    /// Version: 1.0
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_NativeBundle_GetAppId() -> *mut ::core::ffi::c_char;
    /// Obtains the appIdentifier of application. AppIdentifier does not change along the application lifecycle,
    /// including version updates, certificate changes, public and private key changes, and application transfer.
    /// After utilizing this interface, to prevent memory leaks,
    /// it is necessary to manually release the pointer returned by the interface.
    ///
    ///
    /// # Returns
    ///
    /// * Returns the newly created string that indicates app identifier information,
    /// if the returned object is NULL, it indicates creation failure.
    /// The possible cause of failure could be that the application address space is full,
    /// leading to space allocation failure.
    ///
    /// Available since API-level: 11
    ///
    /// Version: 1.0
    #[cfg(feature = "api-11")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-11")))]
    pub fn OH_NativeBundle_GetAppIdentifier() -> *mut ::core::ffi::c_char;
    /// Obtains information of the entry mainElement based on the current application, including bundle name,
    /// module name, and ability name.
    /// After utilizing this interface, to prevent memory leaks,
    /// it is necessary to manually release the pointer returned by the interface.
    ///
    ///
    /// # Returns
    ///
    /// * Returns the newly created OH_NativeBundle_ElementName object, if the returned object is NULL,
    /// it indicates creation failure. The possible cause of failure could be that the application address space is full,
    /// leading to space allocation failure.
    ///
    /// Available since API-level: 13
    #[cfg(feature = "api-13")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-13")))]
    pub fn OH_NativeBundle_GetMainElementName() -> OH_NativeBundle_ElementName;
    /// Obtains the compatible device type of the current application.
    /// After utilizing this interface, to prevent memory leaks,
    /// it is necessary to manually release the pointer returned by the interface.
    ///
    ///
    /// # Returns
    ///
    /// * Returns the newly created string that indicates the compatible device type,
    /// if the returned object is NULL, it indicates creation failure.
    /// The possible cause of failure could be that the application address space is full,
    /// leading to space allocation failure.
    ///
    /// Available since API-level: 14
    ///
    /// Version: 1.0
    #[cfg(feature = "api-14")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-14")))]
    pub fn OH_NativeBundle_GetCompatibleDeviceType() -> *mut ::core::ffi::c_char;
    /// Obtains the application debug mode.
    ///
    /// # Arguments
    ///
    /// * `isDebugMode` - Indicates whether the application is in debug mode.
    ///
    /// # Returns
    ///
    /// * Returns true if call successful, false otherwise.
    ///
    /// Available since API-level: 20
    #[cfg(feature = "api-20")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
    pub fn OH_NativeBundle_IsDebugMode(isDebugMode: *mut bool) -> bool;
    /// Obtains the module metadata array of the current application.
    /// After utilizing this interface, to prevent memory leaks,
    /// it is necessary to manually release the pointer returned by the interface.
    ///
    /// # Arguments
    ///
    /// * `size` - Indicates the module metadata array size.
    ///
    /// # Returns
    ///
    /// * Returns the newly created module metadata array, if the returned object is NULL,
    /// it indicates creation failure. The possible cause of failure could be that the application address space is full,
    /// leading to space allocation failure.
    ///
    /// Available since API-level: 20
    #[cfg(feature = "api-20")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-20")))]
    pub fn OH_NativeBundle_GetModuleMetadata(
        size: *mut usize,
    ) -> *mut OH_NativeBundle_ModuleMetadata;
    /// Obtain a list of ability that support opening files in a certain format.
    ///
    /// `ohos.permission.GET_ABILITY_INFO`.
    /// # Arguments
    ///
    /// * `fileType` - Indicates the file type.
    ///
    /// * `abilityResourceInfo` - Indicates the ability resource array.
    ///
    /// * `size` - Indicates the ability resource array size.
    ///
    /// # Returns
    ///
    /// * Returns [`BUNDLE_MANAGER_ERROR_CODE_NO_ERROR`] if the call is successful.
    /// Returns [`BUNDLE_MANAGER_ERROR_CODE_PERMISSION_DENIED`] if the caller has no correct permission.
    ///
    /// Available since API-level: 21
    #[cfg(feature = "api-21")]
    #[cfg_attr(docsrs, doc(cfg(feature = "api-21")))]
    pub fn OH_NativeBundle_GetAbilityResourceInfo(
        fileType: *mut ::core::ffi::c_char,
        abilityResourceInfo: *mut *mut OH_NativeBundle_AbilityResourceInfo,
        size: *mut usize,
    ) -> BundleManager_ErrorCode;
}
