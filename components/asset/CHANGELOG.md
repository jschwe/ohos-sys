# Changelog

## 0.1.1

- Add `api-22` and `api-23` feature flags (no new symbols).

## 0.1.0

- Initial AssetStoreKit (Asset NDK) bindings for API-11 through API-21.
  Includes `OH_Asset_QuerySyncResult` and `Asset_SyncResult` at API-20.
